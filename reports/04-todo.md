# 04 – Todo

Controlled development stages. Statuses: `DONE`, `IN PROGRESS`, `PENDING`,
`BLOCKED` (with reason).

## Stage 0 – Foundation (current)

| # | Task | Status | Notes |
| --- | --- | --- | --- |
| 0.1 | Initialize clean React project (Vite) | DONE | React 19 + Vite 8, oxlint |
| 0.2 | Verify baseline build | DONE | `npm run build` passes |
| 0.3 | Create initial source structure | DONE | components/, features/, hooks/, lib/, pages/ |
| 0.4 | Create `reports/` documentation system | DONE | 6 documents |
| 0.5 | Create root README.md | DONE | Project-focused |
| 0.6 | Create DEVELOPMENT_RULES.md | DONE | Project rules |
| 0.7 | Write initial architecture document | DONE | DRAFT, no schema |
| 0.8 | Add `@` path alias + `.env.example` + `.gitignore` env rules | DONE | Config recorded in log |
| 0.9 | Record everything in log/decisions/todo | DONE | 2026-08-11 entry |

## Stage 1 – Architecture review (in progress)

| # | Task | Status | Notes |
| --- | --- | --- | --- |
| 1.1 | Finalize exact frontend libraries (router, state, styling, animation) | DONE | D-014…D-018 |
| 1.2 | Finalize backend framework + deployment target | DONE | D-019 (Hono), D-020 (Vercel) |
| 1.3 | Finalize student identity model | DONE | D-005: records, not Auth users |
| 1.4 | Finalize activity engine contract & activity-type schema | DONE | `05-activity-engine-design.md`; D-021 |
| 1.5 | Finalize 3-of-100 question selection algorithm | DONE | D-022; design doc §8 |
| 1.6 | Update architecture doc to a reviewed baseline | DONE | `01-initial-architecture.md` v1.1 |
| 1.7 | Finalize scoring formula | DONE | D-023: base 100 − hints − attempts − overtime; floor 0, max 300; hint/attempt values configurable |
| 1.8 | Finalize leaderboard aggregation strategy | DONE | D-010 (2026-08-11) |
| 1.9 | Finalize certificate generation approach | DONE | D-011 (2026-08-11) |
| 1.10 | Install chosen frontend libraries (review-approved) | DONE | 2026-08-14: `react-router@7.18.2`, `zustand@5.0.15`, `@tanstack/react-query@5.101.4`, `motion@13.1.0`, `tailwindcss@4.3.3` + `@tailwindcss/vite@4.3.3`. Wired: QueryClientProvider + RouterProvider in `main.jsx`; `/` → existing App; 8 lazy placeholder routes (`src/router.jsx`); minimal ui-store; Tailwind v4 via `@tailwindcss/vite` + `@import 'tailwindcss'` (design tokens preserved). `npm test` 785/785, lint clean, build clean, bundle probe 0. `reports/21-task-1.10-frontend-libraries.md`. |

## Stage 2 – Database schema design (in progress)

| # | Task | Status | Notes |
| --- | --- | --- | --- |
| 2.1 | Design complete PostgreSQL schema (21 tables, 19 domains) | DONE | `06-database-architecture.md` |
| 2.2 | Document RLS/security strategy (3 access modes) | DONE | §9; D-027/D-028 |
| 2.3 | Document index strategy with rationale | DONE | §7 |
| 2.4 | Document leaderboard/progress/certificate strategies | DONE | §10–12; D-029/D-030/D-031 |
| 2.5 | Document JSONB vs relational split | DONE | §8; D-026 |
| 2.6 | Document Storage / Realtime / Free-Tier / lifecycle | DONE | §13–16 |
| 2.7 | Document 3-question session data flow | DONE | §17 |
| 2.8 | Record all DB decisions in `03-decisions.md` | DONE | D-024…D-040 |
| 2.9 | Human/architect review of the design | DONE | **APPROVED / READY FOR MIGRATION** (2026-08-11 correction pass). Scoring formula finalized, stream/level composite FKs added, login security documented. SQL NOT written. |
| 2.10 | Write SQL migration files (DDL) for the approved schema | DONE | `supabase/migrations/0001_initial_schema.sql` created/validated (2026-08-11). Local PG18 cluster validation passed (21 tables, RLS, composite FKs, constraints). NOT applied to Supabase. |
| 2.11 | Create Supabase project, apply migrations, RLS policies, seed base data | DONE | 2026-08-11: Linked existing project `fmauqixvdpdgrghuapfs` (STEM Quest Project, Northeast Asia/Seoul); verified empty; `supabase db push` applied 0001+0002; verified 21 tables + 1 view, RLS on all 21, seed counts (4/20/10/4/8), anon RLS spot check. |
| 2.12 | Storage buckets + policies + upload validation | DONE | 2026-08-11: `0003_storage_buckets.sql` — private `student-avatars` (≤200 KB, jpeg/png/webp) + private `question-media` (≤1 MB, jpeg/png/webp); bucket-level size/MIME enforcement; admin-only authenticated SELECT policies via `is_admin()`; default-deny otherwise; no `certificates` bucket. Verified on remote. |
| 2.13 | Final consistency pass: plugin scoring boundary + correctness representation | DONE | 2026-08-11; D-041; plugins report inputs, central service scores; no SQL |

## Stage 3 – Question content

| # | Task | Status | Notes |
| --- | --- | --- | --- |
| 3.1 | Content model for 2,000 questions (4×5×100) | DONE | 2026-08-11: `reports/07-task-3.1-content-model.md` — full spec (model, lifecycle, taxonomy, grade/difficulty, activity distributions, 20 blueprints, quality rules, anti-dup, hints/feedback/media/timers/scoring, authoring workflow, builder, import, AI, analytics). D-043…D-045. NO questions created, NO DB changes. |
| 3.2 | JSON Schema contracts for the 10 activity types | DONE | 2026-08-12: `schemas/` — 24 schemas (10 payload + 10 correct-answer + 4 common), 70 examples (7/activity), `schemas/validate.py` (3 layers), `reports/08-task-3.2-schemas.md`. Validator PASS (24 meta, 70 examples, 10/10 pairs). NO code/UI/plugins/DB changes. |

## Task 4.1 – Activity Engine core (completed)

| # | Task | Status | Notes |
| --- | --- | --- | --- |
| 4.1 | Build the Activity Engine core | DONE | 2026-08-12: `src/features/activity-engine/` — plugin contract (7 methods), `ActivityRegistry`, AJV schema registry (10 payload client-safe + 10 correct-answer server-only), `validatePayload`/`validateAnswer` routing, scoring-input guard (fraction ∈ [0,1]), hints/feedback/availability, `ActivityEngineError`, client/server facades, semantic-rule infra + catalog, test plugin, 52 tests (`npm test`). `reports/09-task-4.1-activity-engine-core.md`. No game UI, no real activity plugins, no DB changes. |
| 4.2 | First real activity plugin: drag-drop | DONE | 2026-08-12: `src/features/activity-engine/plugins/drag-drop/` — 7-method plugin (incl. `validateMappings` porting catalog rules `mappings-cover-items` / `mappings-zone-exists`), 4 payload-only semantic rules, reference guard (unknown item/zone ids rejected, `ACTIVITY_ANSWER_INVALID`), zero-dependency React renderer (pointer drag + tap-place + keyboard, hints, retry), registers via `registerDragDrop`, demo in `App.jsx`, 37 tests (`npm test` = 89). Bundle probes: correct-answer schema `$id` absent from client bundle, present in server bundle. `reports/10-task-4.2-drag-drop.md`. D-050/D-051. |
| 4.3 | Game Engine core: seeded 3-of-100 selection + session lifecycle | DONE | 2026-08-12: `src/features/game-engine/` — `core/errors.js` (student-safe error model), `core/prng.js` (≥64-bit crypto session seed + mulberry32 + shuffle/pickOne), `core/selection.js` (D-022 §8: strict diversity, fill pass for repeats, last-5-sessions repeat avoidance with fallback, seeded reproducibility, `GAME_INSUFFICIENT_POOL`), `core/session.js` (pure lifecycle: create/guard/current/submit/finish; ordered rounds, immutable transitions, wrong-student/not-current/already-submitted/not-active/invalid-state guards, 0–300 total), `index.js` entry, 26 tests (`npm test` = 115). `reports/11-task-4.3-game-engine-core.md`. D-052/D-053. Answer validation + Central Scoring + repository adapters remain (later tasks). |
| 4.4 | Game Session service: integrate Game Engine + Activity Engine + Central Scoring | DONE | 2026-08-12/13 (service built in Task 4.4; verified by `session-service.test.js` and the demo API). `startSession` returns a client-safe round descriptor (NO correctAnswer), `submitRound` routes validateAnswer → scoringInputs → Central Scoring Service, `finishSession` sums 0–300. Security tests prove forged correctnessFraction/score/overtime are ignored. `reports` index updated. |
| 4.5 | Second activity plugin: matching | DONE | 2026-08-13: `src/features/activity-engine/plugins/matching/` — 7-method plugin + `validatePairs` (catalog port), pure interaction controller, React renderer (tap/click/keyboard/reduced-motion, hints, retry, submit gate), 4 `validatePayload` semantic rules, shared-target + distractor support, partial credit = correct pairs ÷ lefts. Registered in `createDefaultServerActivityEngine()`; demo API seeds matching from Task 3.2 fixtures; App renders matching. Validator now checks the grade9-11 matching pair (11/11). Bundle probe: matching correct-answer schema `$id` absent from client bundle. 245 tests pass. `reports/12-task-4.5-matching.md`. D-054…D-056. |
| 4.6 | Third activity plugin: ordering | DONE | 2026-08-13: `src/features/activity-engine/plugins/ordering/` — 7-method plugin + `validateSequence` (catalog port of `ordering.order-permutation`), 6 `validatePayload` semantic rules, pure interaction controller with gameplay **anchors** (locked positions/items, free-only moves/swaps), React renderer (tap-select / pointer drag / keyboard / reduced-motion, locked-anchor a11y, hints, clear, submit gate), partial credit = correct positions ÷ total. Registered in `createDefaultServerActivityEngine()`. Also fixed the pre-existing flaky `M52` session test (selection-agnostic descriptor-contract assertions; no engine/scoring/security change). Validator 12/12 pairs. Bundle probe: ordering correct-answer schema `$id` absent from client bundle. 299 tests pass (3 consecutive full runs); game-session suite 5×58/58. `reports/13-task-4.6-ordering.md`. D-057…D-059. |
| 4.7 | Fourth activity plugin: sorting | DONE | 2026-08-13: `src/features/activity-engine/plugins/sorting/` — 7-method plugin + `validateAssignments` (catalog port of `sorting.assignments-cover-items`), 3 `validatePayload` semantic rules, pure interaction controller, React renderer (tap-select → tap-group placement, pointer drag enhancement, keyboard, reduced-motion, per-chip remove, hints, clear, submit gate), partial credit = correct assignments ÷ items. Registered in `createDefaultServerActivityEngine()`; demo API seeds sorting from Task 3.2 fixtures; App renders sorting. Validator 12/12 pairs. Bundle probes: sorting correct-answer and payload schema `$id`s absent from client bundle. 358 tests pass (2 consecutive full runs); game-session suite 5×65/65. `reports/14-task-4.7-sorting.md`. |
| 4.8 | Fifth activity plugin: fill-complete | DONE | 2026-08-14: `src/features/activity-engine/plugins/fill-complete/` — 7-method plugin + `validateBlankAnswers` (catalog port of `fill-complete.blanks-referenced`, extended: one answer entry per blank, group/type match, numeric definability), 2 `validatePayload` semantic rules, pure interaction controller, React renderer (template split on `___`, per-type native inputs, prefix/suffix, progress pips + aria-live, hints, clear, submit gate), exact-response normalization (text case-fold, expression whitespace-only, numeric parse + value/tolerance or range). Approved schema consistency fix: `answers` no longer required in the correct-answer schema (D-062) — number-only/expression-only answers valid end-to-end. Registered in `createDefaultServerActivityEngine()`; demo API seeds fill-complete from Task 3.2 fixtures; App renders fill-complete. Validator 24/72/12/12. Bundle probes: fill-complete correct-answer and payload schema `$id`s absent from client bundle. 423 tests pass (2 consecutive full runs); game-session suite 5×73/73. `reports/15-task-4.8-fill-complete.md`. |
| 4.9 | Sixth activity plugin: image-interaction | DONE | 2026-08-14: `src/features/activity-engine/plugins/image-interaction/` — 7-method plugin + `validateImageInteractionAnswer` (catalog port of `image-interaction.hotspots-exist`, extended: required-set coverage, label completeness, no answer beyond the board), 6 `validatePayload` semantic rules, pure interaction controller (normalized 0–100 coordinates, aspect-preserved hit testing, tap + label modes), React renderer (image surface + hotspot overlays, label tray, aria-live progress, hints, clear, submit gate), exact-response scoring (tap = required set, label = placements), partial credit fractional. Registered in `createDefaultServerActivityEngine()`; demo API seeds image-interaction from Task 3.2 fixtures; App renders image-interaction. Also fixed the pre-existing dev-server request bridge (Hono `app.request` returns a plain Response, not a Promise — was crashing on every API request). Validator 24/72/12/12. Bundle probes: image-interaction correct-answer and payload schema `$id`s absent from client bundle. 503 tests pass (2 consecutive full runs); game-session suite 5×503/503; demo API smoke pass (real HTTP, incl. image-interaction rounds). `reports/16-task-4.9-image-interaction.md`. |
| 4.10 | Seventh activity plugin: pattern | DONE | 2026-08-14: `src/features/activity-engine/plugins/pattern/` — 7-method plugin + `validatePatternAnswer` (catalog port of `pattern.acceptable-ids-exist`, extended: attainable construct counts, single-value numeric/text, ordered ranges, non-blank accepted values), 4 `validatePayload` semantic rules, pure interaction controller (candidate selection replace-when-full + typed entry, mutually exclusive paths), React renderer (sequence surface with hidden/trailing answer slots, candidate bank, native number/text entry, aria-live progress, hints, clear, submit gate), three sequence modes (construct-next / fill-missing / complete-sequence), exact-response scoring with EXPLICIT multiple valid solutions (`acceptableIds` is a set, D-069), strict response-shape gate (D-070), schema-literal fill-missing semantics (D-068). Registered in `createDefaultServerActivityEngine()`; demo API seeds pattern from Task 3.2 fixtures; App renders pattern. Validator 24/72/12/12. Bundle probes: pattern correct-answer and payload schema `$id`s absent from client bundle. 577 tests pass (2 consecutive full runs); game-session suite 5×90/90; demo API smoke pass (real HTTP, pattern-only pool, 300/300). `reports/17-task-4.10-pattern.md`. |
| 4.11 | Eighth activity plugin: memory | DONE | 2026-08-14: `src/features/activity-engine/plugins/memory/` — 7-method plugin + `validateMemoryAnswer` (catalog port of `memory.groups-cover-cards`, extended: group sizes match the deck type), 2 `validatePayload` semantic rules (`card-ids-unique`, `deck-size-consistent`), pure interaction controller (two-phase memorize/recall, re-shuffle, reveal budget, capped selection, place/remove groups, completion gate, `buildResponse` → `{ groups: [{ cardIds }] }`), React renderer (memorize deck with `revealSeconds` countdown + auto-transition, re-shuffled recall pool, selection strip, stacked groups, Study again, hints, clear, submit gate), genuine RECALL interaction (pairs = groups of 2, sets = groups of 3–4), unordered-set exact scoring, partial credit = correct groups ÷ total groups, strict response-shape gate. Registered in `createDefaultServerActivityEngine()`; demo API seeds memory from Task 3.2 fixtures (ids 47–49); App renders memory. Validator 24/72/12/12. Bundle probes: memory correct-answer schema `$id` absent from client bundle. 647 tests pass (2 consecutive full runs); game-session suite 5×98/98; demo API smoke pass (real HTTP, memory-only pool, 300/300). `reports/18-task-4.11-memory.md`. |
| 4.12 | Ninth activity plugin: scenario-challenge | DONE | 2026-08-14: `src/features/activity-engine/plugins/scenario-challenge/` — 7-method plugin + `validateScenarioAnswer` (catalog port of `scenario.entry-decision-exists`, extended: optimal-path traversability, acceptable-options integrity), 5 `validatePayload` semantic rules (unique decision/option ids, entry exists, next exists, no self-loop), pure DOM-free navigation-state controller (`buildResponse` → `{ path: [{ decisionId, optionId }] }`), React renderer (mission + progressive decision walk + consequence panel + completion gate + hints), per-step optimal-or-acceptable exact scoring, partial credit = correct steps ÷ path length, strict response-shape gate + entry/reference/continuity/terminal/no-double-back checks, schema-key alias (`scenario` ↔ `scenario-challenge` in the engine schema registry). Registered in `createDefaultServerActivityEngine()`; demo API seeds scenario from Task 3.2 fixtures (ids 50–52); App renders scenario-challenge. Validator 24/72/12/12. Bundle probes: scenario correct-answer schema `$id` + `acceptableOptions` absent from client bundle. 718 tests pass (2 consecutive full runs); game-session suite 5×110/110 (SC1–SC13 incl. nine-type coverage); demo API smoke pass (real HTTP, scenario-only pool, 300/300). `reports/19-task-4.12-scenario-challenge.md`. |
| 4.13 | Tenth & final activity plugin: number-logic | DONE | 2026-08-14: `src/features/activity-engine/plugins/number-logic/` — 7-method plugin + `validateNumberLogicAnswer` (catalog ports of `number-logic.parts-match` + `type-fields`, extended: answer-format-compatible, range-ordered, tolerance-valid, fraction-integer-components, accepted-nonblank, sequence-values-valid, numeric-fields-finite), six answer formats × seven correct-answer types (exact/tolerance/range/fraction/percent/sequence/accepted-set) under one correctness model (one atomic value = one unit; percent = authored number + optional `%`; fraction = lowest-term GCD; sequence element-wise; accepted-set exact, NO eval), multi-step per-part credit (parts-only scoring, D-075), strict response-shape gate (`{ value }`/`{ values }`/`{ parts }`, forged fields rejected), pure DOM-free interaction controller (`buildResponse`), React renderer (single/sequence/multi-part surfaces, fraction two-input, percent suffix, showWork scratchpad, completion gate, aria-live). Registered in `createDefaultServerActivityEngine()`; demo API seeds number-logic from Task 3.2 fixtures (ids 53–55); App renders number-logic. Validator 24/72/12/12. Bundle probe: number-logic correct-answer schema `$id` absent from client bundle. 785 tests pass (2 consecutive full runs); game-session suite 5×120/120 (NL1–NL10 + SC13 now TEN-type coverage); demo API smokes pass (real HTTP: number-logic-only pool 300/300 + mixed 10-activity sessions). `reports/20-task-4.13-number-logic.md`. D-074/D-075. All ten catalogued activity plugins now DONE. |

## Stage 4+ – Implementation (later)

- Student registration, profile editing & progress dashboard — DONE (5.1 + 5.6)
- Stream/level selection UI
- Game engine + session flow
- Activity engine wiring into Game Engine/session flow (drag-drop, matching, ordering, sorting, fill-complete, image-interaction, pattern, memory, scenario-challenge, number-logic plugins — ALL TEN DONE)
- Scoring service
- Leaderboards (Top 10, live)
- Badges & certificates
- Admin panel (Supabase Auth)
- Responsive/exhibition polish

## Stage 5 – Student registration & session foundation

| # | Task | Status | Notes |
| --- | --- | --- | --- |
| 5.1 | Student registration & lightweight session foundation | DONE | 2026-08-15: `src/features/student/` — students remain normal application records (D-005), no Supabase Auth for students; strict-field registration (`{initials,name,school,grade}`, Unicode-safe, grade 6–11 integer); CSPRNG session tokens stored as SHA-256 hashes (`student_sessions` as-is, D-040); real `/student/register` page; Hono `POST /api/student/register`, `GET /api/student/me`, `PUT /api/student/me/avatar`; optional photo via private `student-avatars` bucket (≤200 KB, jpeg/png/webp, backend-only); TanStack Query `/me`; minimal `sessionStorage` token helper; 66 new tests. `npm test` 851/851, lint clean, build clean, HTTP smoke pass, bundle probe 0. NO Supabase migration/RLS/Storage changes. `reports/22-task-5.1-student-registration.md`. |
| 5.2 | Student stream & level selection UI | DONE | 2026-08-15: `src/features/mission/` + `src/pages/StudentMissionPage.jsx` — exactly 4 streams × 5 levels (D-039); `GET /api/student/mission/streams` + `GET /api/student/mission/streams/:streamId/levels` (Bearer-token via `StudentService.getMe`, same session as 5.1); pure access resolver mirroring `GameSessionService.applyUnlockRule` (level 1 open; active grants unlock levels 2–5; D-076); progression status (completed/in-progress/not-started, replayable); grade ≠ level (D-045); read-only memory + service-role Supabase repos (columns match 0001 exactly); real `/student/mission` page (stream picker → level picker → begin → router-state `{streamId,levelId}` → `/student/game` placeholder); `NEXT_STEP_PATH` `/student/game`→`/student/mission`; expired-session 401 guard (redirect to `/student/register`); 38 new tests. `npm test` 889/889, lint clean, build clean, schema validator PASS, HTTP smoke pass. NO Supabase migration/RLS/Storage changes, no new packages. `reports/23-task-5.2-stream-level-selection.md`. |
| 5.3 | Student game UI (session screen) | DONE | 2026-08-15: real `/student/game` screen over the authoritative `GameSessionService` — token-authenticated `/api/student/game/*` (start/resume, current, submit, finish) through `StudentService.getMe` (studentId server-derived), client fetch client + TanStack Query hooks, pure round lifecycle (IDLE→STARTING→PLAYING⇄SUBMITTING→ROUND_RESULT→SESSION_COMPLETE) + minimal Zustand wrapper, session choice storage (refresh recovery), display-only countdown, one `RoundActivity` boundary over all ten renderers, HUD + round-result + completion panels, navigation guard. Shared student-identity facade so registered students (id ≥ 2) can start sessions; ordering demo questions complete the ten-type pool; six plugin entries fixed for Vite SSR loading. 26 new tests. `npm test` 915/915, lint clean, build clean, schema validator PASS, HTTP smoke 20/20, bundle probe 0. NO Supabase changes, no new packages. `reports/24-task-5.3-student-game-ui.md`. |
| 5.4 | Production Supabase integration | DONE | 2026-08-15: production server connects the Tasks 5.1–5.3 student + game system to the real linked Supabase project (`fmauqixvdpdgrghuapfs`) — one service-role client shared by the game-session/student/mission supabase repos (`createProductionApi` + binary-safe HTTP bridge + runner, port 4101); full `.env` support (`node --env-file=.env`, `api:production` + `smoke:production`); progress writes (`student_progress`/`student_level_progress`) documented as deferred to the progression task (user decision). Live smoke exposed + fixed three fake-masked bugs (`createRoundsForSession` unordered, `total_time_ms` iso'd bigint, NaN ids → PostgREST errors) and the latent `getSupabaseServerClient` async bug. 12 new tests (7 repo contract + 5 production API, over a deterministic PostgREST fake). `npm test` 927/927, lint clean, build clean, schema validator PASS, live HTTP smoke 35/35, DB restored to exact baseline, three-way bundle probe clean (no credentials, no answer data; only static error/description prose). NO schema change, no new packages. `reports/25-task-5.4-production-supabase-integration.md`. |
| 5.5 | Student progression backend + level unlock persistence | DONE | 2026-08-15: new `src/features/progression/` — `ProgressionService` is the single authority for the D-076 rule: `assertLevelUnlocked` gates every session start (level 1 open; level N needs N−1 completed same-stream OR an active special-access grant) and `recordCompletion` writes deferred rows on `finishSession` (`student_level_progress` UPSERT on student_id+level_id with best-score max / attempts+1 / first-completed-at preserved, plus the recomputed `student_progress` stream aggregate UPSERT on student_id+stream_id). Idempotent re-finish returns the stored payload with no writes. Game-session repos + contracts gained `progressionRepository` + `LevelRepository.listForStream` (memory + Supabase); the deterministic PostgREST fake gained `upsert`. Mission `resolveLevelAccess`/`buildLevelContext` extended (backward compatible) so a progression-unlocked level renders `available` vs a grant's `special`; `MissionService` threads the same-stream previous level. Special access stays independent (grants never fabricate completions). 39 new tests (service, game-session E2E, supabase repo contracts, mission access); `npm test` 966/966 (3 consecutive runs), lint clean, build clean, schema validator PASS, live HTTP smoke 49/49 (two-level unlock chain verified live), DB restored to exact baseline incl. progression tables, three-way bundle probe clean (prose only). NO schema change, no new packages. `reports/26-task-5.5-student-progression.md`. |
| 5.6 | Student profile + progress dashboard | DONE | 2026-08-16: `StudentService.updateProfile` — identity from the session token only; raw body through the exact registration strict gate (foreign fields like `score`/`studentId` → `400 STUDENT_UNEXPECTED_FIELD`); school resolved to `school_id`; repository touches exactly the editable 0001 columns (never `login_code`). `ProgressionService.getStudentOverview` (new optional `streamRepository`) returns a read-only safe projection: per level exactly `{id,number,name,status,access,replayable}` (no attempts/bestScore), stream-level safe aggregates + `overall` totals. API: `PUT /api/student/me` + `GET /api/student/me/progress` (`createStudentApi({ service, progressionService = null })`, backward compatible); dev + production servers wire `profileProgressionService`. Client `updateProfile`/`getProgress` + Query hooks. Real `/student/profile` page (photo halo, validated edit form, per-stream progress cards + statistics, Continue/Back-to-mission, session-expiry guard); entry points on the register success panel and mission header. 40 new tests; `npm test` 1006/1006 (2 runs), lint clean, build clean, schema validator PASS, live smoke 58/58, DB restored to exact baseline (ilike cleanup removes both smoke schools), three-way bundle probe clean. NO schema change, no new packages. `reports/27-task-5.6-student-profile-progress.md`. |
| 5.7 | Live stream leaderboard | DONE | 2026-08-16: `src/features/leaderboard/` — `LeaderboardService` (recordBestScore/getTopForStream/getAllLeaderboards, exported `isBetterScore` + `TOP_N=10`) with strictly-better upsert over the existing `leaderboard_entries` (0001 §20, NO schema change; score DESC, completion_time_ms ASC NULLS LAST, achieved_at ASC, LIMIT 10 — lower replays never overwrite, D-010/D-029); memory + supabase repos (covered read on `leaderboard_top10_idx`, point read, single `(student_id, stream_id)` upsert); public Hono API `GET /api/student/leaderboards` + `/:streamId` mounted before `/api/student/*` (optional Bearer → `self` highlight only; `toPublicEntry` strips studentId — no private fields); best-effort `finishSession` hook (never 500s; next better attempt repairs). Browser Realtime via `@supabase/realtime-js@^2.112.3` (D-080: single refcounted channel `leaderboard_entries`, runtime `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY`, graceful "Live updates off" when unset — `.env` untouched); `useLiveLeaderboard` invalidates `['leaderboard']`. Real `/leaderboards` page (tabs, self-row highlight, a11y, reduced-motion, reusable `LeaderboardStatus`) + mission-page link. 50 new tests; `npm test` 1056/1056, lint clean, build clean, schema validator PASS, live smoke 69/69 (best-score rule + stream isolation verified live), DB restored to exact baseline incl. `leaderboard_entries`, three-way bundle probe clean. `reports/28-task-5.7-live-leaderboard.md`. |
| 5.8 | Badges & certificates | DONE | 2026-08-16: `src/features/achievements/` — backend-authoritative badges + certificates over the EXISTING `badges`/`student_badges`/`certificates` (0001 §17–§19, 0002 4-badge seed; NO schema change, no new packages). `AchievementsService.awardForCompletion` = the single write path called best-effort by `finishSession` (after the leaderboard hook), gated on a new trusted `getStreamProgress` (`student_progress.stream_completed`); then idempotent badge award (UNIQUE `(student_id, badge_id)`, slug derived server-side) + certificate issue (UNIQUE `(student_id, stream_id)`, `SQ-XXXXXX-XXXXXX` public code via `crypto.randomInt`, unambiguous alphabet, `certificate_code`-collision retry). PDF = **hand-rolled minimal generator** (user decision, D-081: base-14 Helvetica, ASCII width tables, escaping/neutralisation, byte-exact xref) — on demand, never stored (`document_path`/`generated_at` NULL, no bucket). API: `GET /api/student/achievements`, `/certificates`, `/certificates/:id/pdf` (`application/pdf`), public `/certificates/verify/:code` (safe surface only; revoked → list drop + PDF 410 + `valid:false`). Memory + service-role Supabase repos mirror 0001 constraints; fake client gained the 3 tables + `BADGE_SEED`. Real `/student/achievements` page (badge cards, certificate rows + download, verify panel) + TanStack hooks + mission-page link. 50 new tests (47 achievements + 3 finishSession-hook incl. real award flow + best-effort failure); `npm test` 1106/1106 (2 runs), lint clean, build clean, schema validator PASS, live smoke 84/84 (hook award, valid PDF, public verify, ownership isolation, revoke → 410/`valid:false`, unknown code 404, no private fields), DB restored to exact baseline incl. `student_badges` + `certificates`, bundle probe clean (service-role key + PDF-generation secrets 0 files). `reports/29-task-5.8-badges-certificates.md`. |
| 5.9 | Admin panel foundation + Supabase Auth | DONE | 2026-08-16: `src/features/admin/` (server) + `src/features/admin-auth/` (browser). Browser admin login/logout via Supabase Auth (anon key, `persistSession:false`, token mirrored to `sessionStorage`), state-machine controller (`loading/unavailable/unauthenticated/authenticated`, 403-cleanup, no redirect loops), protected `/admin` boundary (`AdminAuthProvider` + `AdminShell`) with placeholder nav (Dashboard, Questions, Students, Progress, Leaderboards, Badges & Certificates, Settings). Server: `AdminService.resolveAdmin` = `auth.getUser(token)` + active `public.admins` row (D-082 — server-side `is_admin()` equivalent, crisp 401 vs 403; student token never grants admin), reusable `requireAdmin` middleware, safe `GET /api/admin/me` (`{admin:{id,displayName,role}}`, no secrets). `createStackedApp` gained `adminApp = null` (production mounts it before `/api/student/*`; demo passes null — no Supabase Auth there). Memory + Supabase repos; fake client gained `admins` + `auth.getUser` + `addFakeAuthUser`/`seedFakeAdmin`. 37 new tests; `npm test` 1143/1143 (2 runs), lint clean, build clean, schema validator PASS, live smoke 91/91 (safe /me, 401 missing/bogus/student-token, 403 non-admin, active-admin row — verified live; smoke's supabase-js Authorization-switch bug found & fixed with a dedicated `authDb`), DB restored to exact baseline incl. `admins` + the Auth user pool, bundle probe clean (service-role key + admin authz identifiers 0 files). NO schema change, no new packages. Question Builder / 2,000 questions NOT built (out of scope). `reports/30-task-5.9-admin-panel-foundation.md`. |
| 5.10 | Admin Question Builder (foundation) | DONE | 2026-08-17: `src/features/admin/questions/` (server) + `src/features/admin-questions/` (browser). Catalogue surface behind `requireAdmin` over a server-authoritative `QuestionService`: GET list (`/questions`, previews only — no correctAnswer/meta), `/catalogue` (4 streams × 5 level bands + activity types), GET `/:id` (full admin surface), POST `/` (create **draft v1**, status forced `draft`, version server-managed), PUT `/:id` (in-place, **version preserved**, published/archived 409 `QUESTION_READONLY_PUBLISHED` D-044), DELETE `/:id` (draft only). **Three-layer validation** (envelope+payload+correct-answer+meta JSON Schema incl. grade 6..11 + `^[a-z][a-z0-9_]{0,31}$` ids + meta `additionalProperties:false`; plugin `validatePayload`; cross-document plugin rules). Memory + service-role Supabase repos (forced `QUESTION_SELECT` joins; row-mapper DTO↔row incl. `meta`). `correctAnswer`/`meta` **server-only**; engine render context **throws `SECURITY_CORRECT_ANSWER_EXPOSED`** → preview renders a student-visible snapshot via new **`createDefaultClientActivityEngine()`** (all 10 plugins, payload-only). Browser: TanStack hooks, `QuestionList` (draft-only delete), `QuestionEditor` (new/edit, lazy template init — SSR fix, collapsed correct-answer block, client advisory validation + server `fields[]` errors), `QuestionPreview` (all 10 kinds), schema-valid starter templates for all 10 types. **Required DB change**: `questions.meta jsonb` (migration `0004`, idempotent, RLS-neutral, D-043 pre-authorized) — **user-approved, pending SQL-editor application** (live builder smoke blocked on it). 59 new tests; `npm test` 1202/1202 (2 runs), lint clean, build clean, schema validator PASS, live smoke 106 checks (builder section verified offline vs production stack: create 201 / invalid 400 3 fields / full HTTP), DB baseline restored & re-verified (incl. cleaning SIGPIPE-orphaned smoke rows), bundle probe clean (credentials/correct-answer `$id`s/admin identifiers 0 files). NO content catalogued; Task 5.11 not started. `reports/31-task-5.10-admin-question-builder.md`. |
| 5.11A | Admin visual question authoring (first four types) | DONE | 2026-08-17: `src/features/admin-questions/visual-editor/` — visual authoring forms for **Drag & Drop, Matching, Ordering, Sorting** replacing the raw-JSON editors for those types. Thin forms write directly into the draft `payload`+`correctAnswer` (single source of truth); pure `model.js` (`nextId` reusing `^[a-z][a-z0-9_]{0,31}$`, entity makers, answer builders that preserve existing mappings/pairs/assignments and re-home dangling references, `buildAnchors` position=rank, `moveInList`); reusable `primitives.jsx`; `registry.js` (`hasVisualForm` + advisory `checkAnswerIntegrity` reusing the **exact** plugin cross-document rules `validateMappings`/`validatePairs`/`validateSequence`/`validateAssignments` — no duplicated/invented UI rules, no correct-answer schema bundled); `index.jsx` exports only the `VisualFormFor` component (fast-refresh clean). `QuestionEditor` reworked: Basic information / Activity editor (visual form for the 4, raw JSON payload for the other 6) / Correct answer (visual types: "derived from the visual form", no raw JSON) / Authoring metadata / Preview / Actions (Validate, Save Draft, Cancel); published/archived read-only (D-044); synchronous draft init from the query cache (SSR-deterministic). `aq-*` styles in `src/pages/admin.css`. NO schema change, no new packages; bundle probes 0 files (credentials/answer `$id`s/admin identifiers), `SECURITY_CORRECT_ANSWER_EXPOSED` guard active, preview snapshot unchanged. 27 new tests (model + SSR + editor integration, validated through the server's `createQuestionValidator`); `npm test` 1229/1229, lint clean, build clean, schema validator PASS, live smoke **106/106** (migration 0004 applied), DB restored to exact baseline. `reports/32-task-5.11a-visual-authoring.md`. 5.11B not started. |
| 5.11B | Admin visual question authoring (remaining six types) | DONE | 2026-08-17: completes visual authoring for **Fill & Complete, Image Interaction, Pattern, Memory, Scenario Challenge, Number / Logic** reusing the 5.11A thin-form/model/registry architecture unchanged. Six new forms: fill-complete (template `___` placeholders + per-blank type/label/prefix/suffix/maxLength + accepted answers → `answers`/`numeric`/`expression` groups emitted only when non-empty); image-interaction (tap vs label, % hotspots circle/rect, required toggles, label placements); pattern (construct-next/fill-missing/complete-sequence, reorderable sequence, candidate bank, candidate/numeric/text rule with per-candidate Acceptable toggles, fresh answers default to all candidates); memory (deck settings, cards with group selectors, group chips; groups live only in `correctAnswer`, sizes via `groupSizeRange`); scenario-challenge (decision tree: entry selector, nodes + options + next-decision links, optimal-option + acceptable toggles; optimalPath traversable by construction); number-logic (problem, answer format, input mode, show-work, single vs multi-part per-part specs; `COMPATIBLE_TYPES` exported from plugin). `model.js` +18 strict helpers (deleted/renamed entities repaired or re-homed, never dangling); `registry.js` maps all ten slugs + `INTEGRITY_RULES` reusing the six plugins' exact cross-document rules (`validateBlankAnswers`/`validateImageInteractionAnswer`/`validatePatternAnswer`/`validateMemoryAnswer`/`validateScenarioAnswer`/`validateNumberLogicAnswer`); `QuestionEditor` offers raw JSON only for unknown types; `NumberField` primitive + `aq-subsection`/`aq-row--element` CSS. NO schema change, no new packages; correct-answer schemas still 0 files in the bundle, guard active. 22 new tests (model via `createQuestionValidator` + SSR + editor integration) → 49 visual-form tests; `npm test` 1251/1251 (2 runs), lint clean, build clean (editor chunk 237.87 kB / gzip 62.80 kB), schema validator PASS, live smoke **106/106 (run twice)**, DB restored to exact baseline. `reports/33-task-5.11b-visual-authoring.md`. Task 5.12 not started. |

| 5.12 | Admin question-media upload (backend-mediated, signed-URL preview, non-destructive lifecycle) | DONE | 2026-08-17: secure uploads into the private `question-media` bucket (0003) through the backend service role only — never in the browser. `security/media.js` (magic-byte sniffing, 1 MB + jpeg/png/webp validation, traversal-proof `sanitizeMediaSegment`, ref `question-media/{owner}/uploads/{uuid}.{ext}`, `collectMediaRefs`); `QuestionMediaService` (upload/url/remove, D-084 ownership-by-path-segment + `isMediaRefInUse` gate, 403 `MEDIA_FORBIDDEN` / 409 `MEDIA_IN_USE`); Hono routes `POST /media`, `GET /media/url`, `DELETE /media` registered before `DELETE /:id`; memory + Supabase media repos (storage upload/`createSignedUrl` TTL/remove) + `isMediaRefInUse` scan; `fake-supabase-client` storage `remove`/`list`; client `uploadMedia`/`mediaUrl`/`removeMedia` + `requestMultipart`; `MediaReferenceEditor` upload/replace/remove with signed-URL preview + SSR-safe placeholder; `QuestionPreview.PreviewImage` thumbnails. 25 new tests (21 server + 4 client/SSR); `npm test` 1276/1276, lint clean, build clean (editor chunk 239.52 kB / gzip 63.46 kB), schema validator PASS, live smoke **122/122** (upload/exists/signed-url/401/400/400 `TOO_LARGE`/400 traversal/403/201-referenced/409 in-use/draft-free/200/removed/no-secrets), DB **and** storage restored to exact baseline (0 objects). NO schema change, no new packages. `reports/34-task-5.12-question-media-upload.md`. D-084. 5.13 (publish/review) done next. |
| 5.13 | Admin question review / approval / publish workflow | DONE | 2026-08-17: server-authoritative lifecycle over the unchanged `status` CHECK — review state lives in `meta.review` (`pending|approved|rejected` + `submittedAt`/`submittedByAdminId`/`reviewedAt`/`reviewerAdminId`/`note`/`version`), every transition writes an immutable `admin_actions` row (0001 §2; NO migration). Service: `submitForReview`/`approve`/`reject` (note required → 400 `REVIEW_NOTE_REQUIRED`)/`publish`/`archive`/`createVersion`/`reviewQueue`/`audit`; create/edit stamp `authoring.createdByAdminId` + audit; update forces `draft`, clears review, preserves source chain; release gates (explanation + ≥1 feedback template + taxonomy + full validation + media integrity); stale-approval guard (409 `APPROVAL_STALE`); invalid transitions → 409 `INVALID_STATE`; clone-on-edit (`meta.sourceQuestionId`/`sourceVersion`; publishing v2 archives v1 with `supersededByVersion`). API: `GET /questions/review` (previews only), `GET /:id/audit`, `POST /:id/submit|approve|reject|publish|archive|versions` (201). Memory + Supabase `adminActionRepository` (id-desc newest-first), fake client `admin_actions`, production wiring + `mediaRepository`. Client: 8 methods + 8 Query hooks. UI: `ReviewQueue` + `ReviewDetail` (admin-only answer, audit trail, Approve/Reject/Publish/Archive) + pages + routes + nav + editor "Submit for review" + list "New version". Student distribution gate (only `published`) locked in by regression tests. 26 new tests (21 lifecycle + 5 client/SSR); `npm test` 1302/1302, lint clean, build clean (editor chunk 240.07 kB / gzip 63.61 kB), schema validator PASS, live smoke **141/141** (create → submit → queue → reject+note → re-submit → approve → publish → clone v2 → publish v2 archives v1 → audit newest-first → `admin_actions` persisted → no secrets), DB + storage restored to exact baseline incl. every `admin_actions` row. NO schema change, no new packages. `reports/35-task-5.13-publish-review-workflow.md`. D-085. |
| 5.14 | Production Question Bank / Content Production | IN PROGRESS | 2026-08-24: Deterministic machine-readable content production pipeline. Batch 1 (182 questions), Batch 2 (100 questions: 85 Science L1 + 15 Technology L1), Batch 3 (100 questions: 86 Science L3 + 14 Engineering L1), Batch 4 (100 questions: 79 Mathematics L1 + 21 Technology L1), & Batch 5 (100 questions: 79 Mathematics L2 + 21 Technology L1) validated, imported, reviewed, approved, and published (582 questions live in production bank across all 4 streams & 10 activity types). All 582 questions transitioned through `draft → pending → approved → published` via `QuestionService` with full `admin_actions` audit trail (2,328 action rows) and media integrity checks. Verification tool `verify-bank.mjs` confirmed 100% published status and 0 lifecycle/media errors. Progress: 582 / 2,000 questions completed. Task 5.15 NOT STARTED. `reports/36-task-5.14-question-bank-content.md`. |






## Backlog / open items

- Animation strategy that is performance-conscious on low-end phones.
- Free Tier budget checks (storage, realtime, DB size) once features exist.
- Live leaderboards now ship (Task 5.7); remaining leaderboard polish
  (see `reports/28-task-5.7-live-leaderboard.md` §14).
- Badges + certificates now ship (Task 5.8); remaining achievements polish:
  admin revoke UI, richer/embedded PDF artwork (see
  `reports/29-task-5.8-badges-certificates.md` §18).

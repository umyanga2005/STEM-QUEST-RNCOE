# STEM QUEST – The Educational Treasure Hunt

## Reports & Documentation Index

This folder holds the living documentation for the STEM QUEST project. Every
report below is updated as the project evolves through its controlled
development stages.

| File | Purpose |
| --- | --- |
| `README.md` | This index. Start here. |
| `00-project-overview.md` | What the product is: vision, scope, game rules, constraints. |
| `01-initial-architecture.md` | Architecture baseline. Do not finalize tables/schema here. |
| `02-development-log.md` | Chronological record of every significant development action. |
| `03-decisions.md` | Architectural and technical decisions with rationale. |
| `04-todo.md` | Completed and pending tasks, stage by stage. |
| `05-activity-engine-design.md` | Detailed design of the interactive Activity Engine (10 types, plugin contract, question selection). |
| `06-database-architecture.md` | Supabase PostgreSQL schema design (21 tables, RLS, indexes, JSONB strategy, storage, realtime, Free Tier). |
| `07-task-3.1-content-model.md` | Complete content model for the 2,000-question bank (lifecycle, taxonomy, blueprints, quality, authoring). |
| `08-task-3.2-schemas.md` | JSON Schema (draft 2020-12) contracts for all 10 activity types + 70 worked examples + 3-layer validator. |
| `09-task-4.1-activity-engine-core.md` | Task 4.1: Activity Engine core — plugin contract, registry, schema registry, validation routing, scoring guard, hints/feedback/availability, client/server facades, 52 tests. |
| `10-task-4.2-drag-drop.md` | Task 4.2: first real activity plugin (drag-drop) — 7-method plugin, `validateMappings` (catalog port), semantic rules, React renderer, a11y, security/bundle boundary, 34 tests. |
| `11-task-4.3-game-engine-core.md` | Task 4.3: Game Engine core — seeded 3-of-100 selection (D-022 §8), session lifecycle state machine, student-safe error model, 26 tests. |
| `12-task-4.5-matching.md` | Task 4.5: second real activity plugin (matching) — 7-method plugin, `validatePairs` (catalog port), pure interaction controller, React renderer, Game Session integration, security/bundle boundary, 50+ tests. |
| `13-task-4.6-ordering.md` | Task 4.6: third real activity plugin (ordering) — 7-method plugin, `validateSequence` (catalog port), anchors, pure interaction controller, React renderer, Game Session integration, M52 flaky-test fix, security/bundle boundary, 54 tests. |
| `14-task-4.7-sorting.md` | Task 4.7: fourth real activity plugin (sorting) — 7-method plugin, `validateAssignments` (catalog port), pure interaction controller, React renderer (tap-select → tap-group, drag, keyboard), Game Session integration, security/bundle boundary, 52 tests. |
| `15-task-4.8-fill-complete.md` | Task 4.8: fifth real activity plugin (fill & complete) — 7-method plugin, `validateBlankAnswers` (catalog port), approved schema fix (`answers` group optional), pure interaction controller, React renderer (per-type native inputs), exact-response scoring, Game Session integration, security/bundle boundary, 57 tests. |
| `16-task-4.9-image-interaction.md` | Task 4.9: sixth real activity plugin (image interaction) — 7-method plugin, `validateImageInteractionAnswer` (catalog port), normalized 0–100 coordinate hit testing, tap + label modes, pure interaction controller, React renderer (image surface + label tray), exact-response scoring, Game Session integration, dev-server bridge fix, security/bundle boundary, 71 tests. |
| `17-task-4.10-pattern.md` | Task 4.10: seventh real activity plugin (pattern) — 7-method plugin, `validatePatternAnswer` (catalog port), three sequence modes (construct-next / fill-missing / complete-sequence), explicit multiple-valid-solutions scoring, pure interaction controller, React renderer (sequence surface + candidate bank + native entry), strict response-shape gate, Game Session integration, security/bundle boundary, 66 tests. |
| `18-task-4.11-memory.md` | Task 4.11: eighth real activity plugin (memory) — 7-method plugin, `validateMemoryAnswer` (catalog port), two-phase memorize/recall (pairs = 2, sets = 3–4), reveal budget, pure interaction controller, React renderer (countdown deck + group builder), unordered-set exact scoring, strict response-shape gate, Game Session integration, security/bundle boundary, 62 tests. |
| `19-task-4.12-scenario-challenge.md` | Task 4.12: ninth real activity plugin (scenario-challenge) — 7-method plugin, `validateScenarioAnswer` (catalog port of `scenario.entry-decision-exists`, extended with optimal-path traversability + acceptable-options integrity), 5 `validatePayload` semantic rules, branched decision-tree interaction (NOT an MCQ), pure navigation-state controller (DOM-free), React renderer (mission + progressive decision walk + consequence panel + completion gate), per-step optimal-or-acceptable exact scoring, strict response-shape gate (`{ path }`), schema-key alias (`scenario` ↔ `scenario-challenge`), Game Session integration, security/bundle boundary, 59 tests. |
| `20-task-4.13-number-logic.md` | Task 4.13: tenth and final real activity plugin (number-logic) — 7-method plugin, `validateNumberLogicAnswer` (catalog ports of `number-logic.parts-match` + `type-fields`, extended with answer-format compatibility, ordered ranges, tolerance validity, integer fraction components, non-blank accepted forms, finite numeric fields), six answer formats × seven correct-answer types (exact/tolerance/range/fraction/percent/sequence/accepted-set), one-correctness-model (one atomic value = one unit), lowest-term fraction reduction, percent-as-authored-number, element-wise sequences, exact accepted-set (NO eval), multi-step per-part credit (parts-only scoring, D-075), pure DOM-free interaction controller, React renderer (single/sequence/multi-part surfaces, fraction two-input, percent suffix, showWork scratchpad, completion gate), strict response-shape gate (`{ value }` / `{ values }` / `{ parts }`), Game Session integration, security/bundle boundary, 57 tests (+10 NL-series, SC13 now ten-type coverage). |
| `21-task-1.10-frontend-libraries.md` | Task 1.10: review-approved frontend libraries installed & verified — `react-router@7.18.2`, `zustand@5.0.15`, `@tanstack/react-query@5.101.4`, `motion@13.1.0`, `tailwindcss@4.3.3` + `@tailwindcss/vite@4.3.3`. Wired: `QueryClientProvider` + `RouterProvider` in `main.jsx`, `/` → existing App, 8 lazy placeholder routes, minimal Zustand ui-store, Tailwind v4 plugin + `@import 'tailwindcss'` (design tokens preserved). Verification: 785/785 tests, lint clean, build clean, bundle probe 0, dev-server HTTP smoke OK. No Admin/Question Builder, no Supabase changes, no production content. |
| `22-task-5.1-student-registration.md` | Task 5.1: student registration & lightweight session foundation — students stay normal application records (no Supabase Auth), strict-field registration (`initials/name/school/grade`, Unicode-safe, grade 6–11), CSPRNG session tokens hashed (SHA-256) in `student_sessions`, real `/student/register` page, Hono `POST /api/student/register` + `GET /api/student/me` + `PUT /api/student/me/avatar`, optional photo via private `student-avatars` bucket, TanStack Query `/me`, minimal `sessionStorage` token helper. Verification: 851/851 tests (66 new student tests), lint clean, build clean, HTTP smoke pass, bundle probe 0. NO Supabase migration/RLS/Storage changes; Task 5.2 not started. |
| `23-task-5.2-stream-level-selection.md` | Task 5.2: student stream & level selection UI — exactly 4 streams × 5 levels (D-039 composite), `GET /api/student/mission/streams` + `/api/student/mission/streams/:streamId/levels`, pure access resolver mirroring `GameSessionService.applyUnlockRule` (level 1 open; active grants unlock levels 2–5), progression status (completed/in-progress/not-started), grade ≠ level, real `/student/mission` page (stream picker → level picker → begin; router-state `{streamId, levelId}` → `/student/game` placeholder), `NEXT_STEP_PATH` → `/student/mission`, expired-session 401 guard. Verification: 889/889 tests (38 new mission tests), lint clean, build clean, schema validator PASS, HTTP smoke pass. NO Supabase changes; Task 5.3 not started. |
| `24-task-5.3-student-game-ui.md` | Task 5.3: student game UI (session screen) — real `/student/game` over the authoritative `GameSessionService`: token-authenticated `/api/student/game/*` (start/resume, current, submit, finish) via `StudentService.getMe` (studentId server-derived), client fetch client + Query hooks, pure round lifecycle (IDLE→STARTING→PLAYING⇄SUBMITTING→ROUND_RESULT→SESSION_COMPLETE) + minimal Zustand wrapper, session choice storage, display-only countdown, one `RoundActivity` boundary over all ten renderers, HUD + result + completion panels, navigation guard. Shared student-identity facade; ordering demo content completes the ten-type pool; six plugin entries fixed for Vite SSR. Verification: 915/915 tests (26 new game-session tests), lint clean, build clean, schema validator PASS, HTTP smoke 20/20, bundle probe 0. NO Supabase changes, no new packages; Task 5.4 not started. |
| `25-task-5.4-production-supabase-integration.md` | Task 5.4: production Supabase integration — one service-role client (`getSupabaseServerClient`, async fix) shared by the existing game-session/student/mission supabase repos wires Tasks 5.1–5.3 to the real linked project (`fmauqixvdpdgrghuapfs`): `createProductionApi` + binary-safe HTTP bridge (multipart avatars) + `runProductionServer` (port 4101), full `.env` support (`node --env-file=.env`, `api:production` + `smoke:production`). Deterministic PostgREST fake + 12 new tests (7 repo contract, 5 production API). Live smoke surfaced + fixed three fake-masked bugs (unordered inserted rounds, `total_time_ms` bigint iso'd, NaN ids → PostgREST errors) + the latent client bug. Progress writes deferred to the progression task (user decision). Verification: 927/927 tests, lint clean, build clean, schema validator PASS, live HTTP smoke 35/35, live DB restored to exact baseline, three-way bundle probe clean (no credentials, no answer data; only static error/description prose). NO schema change, no new packages; Task 5.5 not started. |
| `26-task-5.5-student-progression.md` | Task 5.5: student progression backend + level unlock persistence — new `src/features/progression/` with `ProgressionService` as the single D-076 authority: `assertLevelUnlocked` gates every session start (level 1 open; level N needs N−1 completed same-stream OR an active special-access grant) and `recordCompletion` writes the deferred rows on `finishSession` (`student_level_progress` UPSERT on student_id+level_id with best-score max / attempts+1 / first-completed-at preserved + recomputed `student_progress` stream aggregate UPSERT on student_id+stream_id). Idempotent re-finish; game-session repos gained `progressionRepository` + `LevelRepository.listForStream`; fake PostgREST gained `upsert`; mission resolver/service render progression unlocks as `available` (backward compatible). 39 new tests; 966/966 (3 runs), lint/build/schema PASS, live smoke 49/49 (two-level chain verified live), DB restored to exact baseline incl. progression tables, three-way bundle probe clean (prose only). NO schema change, no new packages; Task 5.6 not started. |
| `27-task-5.6-student-profile-progress.md` | Task 5.6: student profile + progress dashboard — `StudentService.updateProfile` (identity from the session token only; raw body through the exact registration strict gate, foreign fields like `score`/`studentId` → `400 STUDENT_UNEXPECTED_FIELD`; school → `school_id`; editable 0001 columns only, never `login_code`); `ProgressionService.getStudentOverview` (new optional `streamRepository`) safe projection — per level exactly `{id,number,name,status,access,replayable}` (no attempts/bestScore), stream-level safe aggregates + `overall`; API `PUT /api/student/me` + `GET /api/student/me/progress` (`createStudentApi({ service, progressionService = null })`); client `updateProfile`/`getProgress` + Query hooks; real `/student/profile` page (photo halo, validated edit form, per-stream cards + statistics, Continue/Back-to-mission, session-expiry guard) with entry points on the register panels and mission header. 40 new tests; 1006/1006 (2 runs), lint/build/schema PASS, live smoke 58/58, DB restored to exact baseline, three-way bundle probe clean. NO schema change, no new packages; Task 5.7 not started. |
| `28-task-5.7-live-leaderboard.md` | Task 5.7: live stream leaderboards — four stream Top-10 boards over the existing `leaderboard_entries` (0001 §20, NO schema change) with strictly-better upsert (D-010/D-029: score desc, completion-time asc nulls-last, achieved-at asc, LIMIT 10; lower replays never overwrite); `LeaderboardService` + memory/supabase repos (one covered read + one point read + single `(student_id, stream_id)` upsert), public Hono API `GET /api/student/leaderboards(/`:streamId)` (optional token → `self` highlight only; `toPublicEntry` strips studentId), best-effort `finishSession` hook (never 500s), browser Realtime via `@supabase/realtime-js` (D-080 single refcounted channel, `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` runtime config, graceful "Live updates off"); real `/leaderboards` page (tabs, self-row highlight, a11y, reduced-motion) + mission-page link. 50 new tests; 1056/1056, lint/build/schema PASS, live smoke 69/69 (best-score rule verified live), DB restored to exact baseline, three-way bundle probe clean. `.env` untouched; Task 5.8 not started. |
| `29-task-5.8-badges-certificates.md` | Task 5.8: badges & certificates — backend-authoritative achievements over the existing `badges`/`student_badges`/`certificates` (0001 §17–§19, 0002 4-badge seed; NO schema change, no new packages). `AchievementsService.awardForCompletion` is the single write path (best-effort `finishSession` hook, gated on the trusted `student_progress.stream_completed` via new `getStreamProgress`); idempotent badge award (slug derived server-side) + certificate issue with `SQ-XXXXXX-XXXXXX` public code (`crypto.randomInt`, unambiguous alphabet, `certificate_code`-collision retry). Certificate PDF is a **hand-rolled minimal generator** (user decision, D-081 — base-14 Helvetica, ASCII width tables, byte-exact xref) generated on demand and never stored. API: student achievements/certificates/PDF (`application/pdf`) + public `verify/:code` (safe surface only; revoked → list drop, PDF 410, `valid:false`). Real `/student/achievements` page + TanStack hooks + mission link. 50 new tests; 1106/1106, lint/build/schema PASS, live smoke 84/84 (hook award, valid PDF, ownership isolation, revoke flow), DB restored to exact baseline incl. `student_badges`+`certificates`, bundle probe clean (service-role key + PDF secrets 0 files). Task 5.9 not started. |
| `30-task-5.9-admin-panel-foundation.md` | Task 5.9: admin panel foundation + Supabase Auth — browser-side admin login/logout with a real state machine, protected `/admin` boundary, server-side `requireAdmin`, safe `GET /api/admin/me`, and an admin shell with placeholder nav (Dashboard, Questions, Students, Progress, Leaderboards, Badges & Certificates, Settings). `AdminService.resolveAdmin` = `auth.getUser(token)` + active `public.admins` row (D-082: server-side `is_admin()` equivalent, 401 vs 403 crisp; student token never grants admin). Browser uses the anon key (`persistSession:false`), token mirrored to `sessionStorage`; service-role stays server-only. New `src/features/admin/` + `src/features/admin-auth/`; `createStackedApp` gains `adminApp` (production mounts it before `/api/student/*`; demo passes null). NO schema change, no new packages. 37 new tests; 1143/1143, lint/build/schema PASS, live smoke 91/91 (auth 401/403 matrix + safe /me verified live; smoke's supabase-js Authorization-switch bug found & fixed with a dedicated authDb), DB restored to exact baseline incl. `admins` + Auth user pool, bundle probe clean (service-role key + admin authz identifiers 0 files). Question Builder/content NOT built (out of scope). |
| `31-task-5.10-admin-question-builder.md` | Task 5.10: admin question builder foundation — catalogue surface (`GET /questions`, `/catalogue`, `/` + `POST`, `PUT /:id`, `DELETE /:id`) behind `requireAdmin` over a server-authoritative `QuestionService` with three-layer validation (envelope/payload/correct-answer/meta JSON Schema + plugin `validatePayload` + cross-document plugin rules, incl. `^[a-z][a-z0-9_]{0,31}$` identifiers, grade 6..11, `meta` `additionalProperties:false`). Authoring lifecycle: create forces `draft` v1, update preserves `version`, published/archived write-protected (D-044); `correctAnswer`/`meta` server-only (render context throws `SECURITY_CORRECT_ANSWER_EXPOSED`; preview renders a student-visible snapshot). New `src/features/admin/questions/` (server) + `src/features/admin-questions/` (browser: list, new/edit editor, per-type live preview, TanStack hooks) + `createDefaultClientActivityEngine()` registering all 10 plugins; 10 schema-valid templates. One required DB change: `questions.meta jsonb` (migration `0004`, D-043 pre-authorized, RLS-neutral, user-approved — pending SQL-editor application). 59 new tests; 1202/1202, lint/build/schema PASS, prod-stack offline builder verification (create 201 / invalid 400 with 3 fields / full HTTP), live smoke extended to 106 checks (builder section blocked until 0004 applied), DB baseline restored (incl. orphaned smoke rows cleaned), bundle probe clean. Task 5.11 not started. |
| `32-task-5.11a-visual-authoring.md` | Task 5.11A: admin visual question authoring for the first four activity types — Drag & Drop, Matching, Ordering, Sorting. New `src/features/admin-questions/visual-editor/`: thin visual forms writing directly into the draft `payload`+`correctAnswer` (single source of truth), a pure DOM-free `model.js` (id allocation `^[a-z][a-z0-9_]{0,31}$`, entity makers, answer builders that preserve/re-home mappings, `buildAnchors` position=rank, `moveInList`), reusable `primitives.jsx`, and a `registry.js` exposing `hasVisualForm` + advisory `checkAnswerIntegrity` that reuses the exact plugin cross-document rules (`validateMappings`/`validatePairs`/`validateSequence`/`validateAssignments` — no duplicated/invented UI rules, no correct-answer schema in the bundle). `QuestionEditor` reworked into Basic information / Activity editor / Correct answer (visual types show a "derived from the visual form" note, no raw JSON) / Authoring metadata / Preview / Actions; raw JSON editors kept for the other six types; published/archived read-only (D-044). NO schema change, no new packages; bundle probes still 0 files (credentials/answer `$id`s/admin identifiers), `SECURITY_CORRECT_ANSWER_EXPOSED` guard active, preview snapshot unchanged. 27 new tests (model + SSR + editor integration) validated through the server's `createQuestionValidator`; 1229/1229, lint/build/schema PASS, live smoke 106/106 (migration 0004 applied), DB restored to exact baseline. 5.11B (remaining six forms) not started. |
| `33-task-5.11b-visual-authoring.md` | Task 5.11B: completes visual authoring for the remaining six activity types — Fill & Complete, Image Interaction, Pattern, Memory, Scenario Challenge, Number / Logic. Six new thin forms under `src/features/admin-questions/visual-editor/` reusing the 5.11A architecture (forms write directly into draft `payload`+`correctAnswer`; model builders are strict about references — deleted/renamed entities repaired or re-homed, never dangling). Registry now maps all ten slugs to visual forms and reuses the six plugins' exact cross-document rules (`validateBlankAnswers`/`validateImageInteractionAnswer`/`validatePatternAnswer`/`validateMemoryAnswer`/`validateScenarioAnswer`/`validateNumberLogicAnswer`) as advisory `checkAnswerIntegrity`; `QuestionEditor` offers raw JSON only for unknown types. Contract details preserved: fill-complete answers/numeric/expression groups optional-but-covered; image tap vs label (requiredHotspots vs placements); pattern multiple-valid-solutions via acceptableIds and strict no-fallback answers; memory groups live only in `correctAnswer` (groupSizeRange pairs=2/sets=3–4, even-pairs check); scenario is a decision tree (optimalPath traversable by construction, acceptableOptions); number-logic per-part specs (D-075 parts-only, neutral top-level `type` per `partial-credit.json`) with `COMPATIBLE_TYPES` exported from the plugin. NO schema change, no new packages; correct-answer schemas still absent from the bundle (probe 0 files), guard active. 22 new tests (model via `createQuestionValidator` + SSR + editor integration) — 49 visual-form tests; 1251/1251, lint/build/schema PASS, live smoke 106/106 (run twice), DB restored to exact baseline. Task 5.12 (media upload + publish/review) not started. |
| `34-task-5.12-question-media-upload.md` | Task 5.12: secure question-media upload for the Admin Question Builder — backend-mediated uploads to the private `question-media` bucket (0003), never the service role in the browser. New `security/media.js` (magic-byte sniffing JPEG/PNG/WebP, 1 MB + MIME + content validation, traversal-proof `sanitizeMediaSegment`, `buildQuestionMediaPath` → `question-media/{owner}/uploads/{uuid}.{ext}`, recursive `collectMediaRefs`); `QuestionMediaService` upload/url/remove with ownership-by-path-segment + `isMediaRefInUse` gate (D-084, no media table — 403 `MEDIA_FORBIDDEN`, 409 `MEDIA_IN_USE`); Hono `POST /media`, `GET /media/url`, `DELETE /media` (before `/:id`); memory + Supabase media repos + client `uploadMedia`/`mediaUrl`/`removeMedia`; `MediaReferenceEditor` upload/replace/remove with signed-URL preview and SSR-safe placeholder; `QuestionPreview.PreviewImage` thumbnails. 25 new tests (21 server + 4 client/SSR); 1276/1276, lint/build/schema PASS, live smoke 122/122 (upload → exists → signed URL → 401/400/400 TOO_LARGE/400 traversal/403 → 201 draft → 409 in-use → draft-free → 200 → removed → no secrets), DB + storage both restored to exact baseline (0 objects). NO schema change, no new packages. |
| `35-task-5.13-publish-review-workflow.md` | Task 5.13: admin question review/approval/publish workflow — server-authoritative lifecycle (`draft → pending → approved → published → archived`) over the unchanged `status` CHECK + `meta.review` (state/submittedAt/submittedByAdminId/reviewedAt/reviewerAdminId/note/version), release gates (explanation + feedback templates + taxonomy + full validation + media integrity), immutable `admin_actions` audit trail (newest-first, id-desc tie-break), stale-approval guard (409 `APPROVAL_STALE`), note-required reject (400 `REVIEW_NOTE_REQUIRED`), clone-on-edit (`meta.sourceQuestionId`/`sourceVersion`, publishing v2 archives v1), API `GET /review`, `GET /:id/audit`, `POST /:id/submit|approve|reject|publish|archive|versions`, review queue + detail UI (admin-only answer, audit trail), editor "Submit for review" + list "New version". 26 new tests (21 server + 5 client/SSR); 1302/1302, lint/build/schema PASS, live smoke 141/141 (submit → queue → reject+note → re-submit → approve → publish → clone v2 → publish v2 archives v1 → audit newest-first → `admin_actions` persisted → no secrets), DB + storage restored to exact baseline incl. every `admin_actions` row. NO schema change, no new packages. D-085 (no reviewer role split). Task 5.14 (question-bank content) not started. |
| `36-task-5.14-question-bank-content.md` | Task 5.14: production question-bank content — **IN PROGRESS (182/2,000 live)**. Batch 1: 182 release-ready questions (mathematics 102, science 55, technology 12, engineering 13; all ten activity types; D1–D4; 55 approved topic pairs) entered the live bank through the sanctioned pipeline only: `content-validator.mjs` (same `createQuestionValidator`, Q1–Q16 machine subset + exact/near-dup + blueprint exceedance) → `QuestionService.create` drafts → submit/approve/publish via real review flow with 728 `admin_actions` audit rows; 10 generated diagram PNGs uploaded at the correct `question-media/` storage-prefix paths and integrity-verified at publish; idempotent by canonical contentHash (duplicate-run incident fixed + orphaned audit rows swept per the smoke convention). Gates: 1302/1302 tests, lint clean, build PASS, schemas validate.py PASS, verify-bank VERIFIED_OK, runtime eligible-pool check returns published rows. Live smoke (141/141) intentionally NOT re-run — it asserts an empty baseline and sweeps `@stem-quest.test` admins (documented in report). NO schema change, no new packages, no `src/` changes. Task 5.15 not started. |

## How to use these reports

- **Before coding a new stage**, read `00-project-overview.md`,
  `01-initial-architecture.md`, and `04-todo.md`.
- **After every significant action**, append an entry to `02-development-log.md`.
- **When a decision is made**, record it in `03-decisions.md` (with status).
- **Always** follow `DEVELOPMENT_RULES.md` at the repository root.

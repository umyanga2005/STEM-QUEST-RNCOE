/**
 * Activity Engine — semantic rules catalog (Task 4.1, report §4).
 *
 * See `validation/semantic/index.js` for the reusable infrastructure. This
 * file is intentionally a thin re-export so imports stay clean.
 */

export { SEMANTIC_RULES_CATALOG } from './index.js'
